import React from "react";
import { LoaderIcon } from "react-hot-toast";

function Spinner() {
  return (
    <LoaderIcon
      style={{
        width: 18,
        height: 18,
      }}
    />
  );
}

export default Spinner;
